<RadzenTabs @bind-SelectedIndex="selectedIndex">
    <Tabs>
        <RadzenTabsItem Text="Cambio de Destinatario">

            <ChangeReceiver IncidentId="@IncidentId" />

        </RadzenTabsItem>
        <RadzenTabsItem Text="Devolver al remitente" Disabled="@(IncidentTypeId == 3)">

            <ReturnToSender IncidentId="@IncidentId" />

        </RadzenTabsItem>
    </Tabs>
</RadzenTabs>